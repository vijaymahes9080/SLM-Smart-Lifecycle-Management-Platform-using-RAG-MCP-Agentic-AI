import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
  basePath: "/SLM-Smart-Lifecycle-Management-Platform-using-RAG-MCP-Agentic-AI",
};

export default nextConfig;
